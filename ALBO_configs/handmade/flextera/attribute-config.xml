<?xml version="1.0" encoding="UTF-8"?>
<root>
	<!-- 
		This is the configuration file for fa-commons-attribute.jar package.
	 -->
	 
	<!-- 
		Change localhost to the hostname where the services are actually deployed.
		Parameters is required. 
	-->
	<corews>http://localhost:9085/corews/corews</corews>
	<adminws>http://localhost:9085/adminws/adminws</adminws>	
	<!-- 
		Sets the time to live for an element before it expires.
    	i.e. The maximum time between creation time and when an element expires.
    	Parameter is optional. The default value is 60 sec.
     -->
	<timeToLiveSeconds>60</timeToLiveSeconds>
</root>
